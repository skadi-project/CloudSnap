/**
 * Точка входа Electron. Вся логика вынесена в src/main/.
 * Этот файл — тонкий shim для обратной совместимости с package.json
 * ("main": "main.js" → компилируется из этого файла).
 */
import './src/main/index';